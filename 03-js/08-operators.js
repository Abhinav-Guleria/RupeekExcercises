// +, -, *, /, %
// ++, --

// <, >, <=, >=, ==, !=

// !, &&, ||

// condition ? on_true : on_false

// ===, !==
console.log( 1 == 1 );
console.log( 1 === 1 );

console.log( 1 == '1' );
console.log( 1 === '1' ); // type-safe equality check

// x !== y <=> !( x === y )